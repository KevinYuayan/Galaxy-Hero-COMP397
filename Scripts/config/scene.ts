module config {
    export enum Scene {
        START,
        INSTRUCTIONS,
        INTERMISSION,
        LEVEL1,
        LEVEL2,
        LEVEL3,
        WIN,
        OVER,
        SCENE_COUNT
    }
}